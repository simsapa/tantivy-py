[build-system]
requires = ["maturin>=0.13,<0.14"]
build-backend = "maturin"

[project]
name = "tantivy"
version = "0.20.2-pre.5"
requires-python = ">=3.7"

[project.optional-dependencies]
dev = [
    "nox",
]

[tool.maturin]
bindings = "pyo3"
